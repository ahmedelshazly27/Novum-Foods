project_id = "novum-foods"

# Called from the public website without a Supabase session, so no JWT.
# The function does its own validation, origin check and rate limiting.
[functions.contact]
verify_jwt = false
